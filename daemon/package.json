{
  "name": "@etzhayyim/playwright-daemon",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Local Playwright daemon — long-polls com.etzhayyim.apps.playwright actor for pending actions, executes via Playwright, resolves valueRef via macOS Keychain / 1Password / env / literal. Credentials never leave the Mac.",
  "type": "module",
  "bin": { "playwright-daemon": "dist/index.js" },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "install-browsers": "playwright install chromium"
  },
  "dependencies": { "playwright": "^1.48.0" },
  "devDependencies": { "@types/node": "^22.0.0", "tsx": "^4.22.4", "typescript": "^5.6.0" },
  "engines": { "node": ">=20" }
}
